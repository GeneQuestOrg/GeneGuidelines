# Development / test-only dependencies.
# Production deps live in requirements.txt; install both for the full toolchain:
#   pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Test framework + plugins used across backend/tests and backend/content/tests.
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=6.0
pytest-mock>=3.14
respx>=0.22
vcrpy>=7.0

# Local dev convenience: honcho runs the Procfile (`make dev`).
honcho>=2.0
